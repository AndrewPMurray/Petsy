function Account(user) {
    const userTest = Object.values(user)
    console.log(userTest[0].email
    )


    return (
        <>
            My Account
        </>
    )
}

export default Account;
